{
  "name": "fluxmaster",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "vitest run --config e2e/vitest.config.ts --dir e2e",
    "typecheck": "tsc -b",
    "dev": "tsx packages/cli/src/index.ts",
    "dev:server": "tsx packages/server/src/index.ts",
    "fluxmaster": "node packages/cli/dist/index.js",
    "start": "node packages/server/dist/index.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
